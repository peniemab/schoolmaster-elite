// tailwind.config.ts
const config = {
  theme: {
    extend: {
      colors: {
        school: {
          navy: '#1e293b',    // Bleu Marine (Confiance)
          emerald: '#10b981', // Vert Émeraude (Finance/Réussite)
          gold: '#f59e0b',    // Or (Premium)
          slate: '#f8fafc',   // Blanc Cassé (Fond de page)
        }
      },
    },
  },
}